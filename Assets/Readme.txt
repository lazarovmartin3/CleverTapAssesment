There is two squares on the display.

The button "Click Me!" is the trigger. When that button is pressed, 
it check on what platform is run the app. Then switch to the coresponding image.
The button also trigger the weather app. It donwload the information from the provided API,
and show the information on the top square.

There is also two tests. Craeted in Unity Test Runner. 
The first one checks if the button,
The second one compare if the information from the server is written on the display.